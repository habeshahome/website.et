import { memo } from 'react';
import Button from '@mui/material/Button';

const AddUser = () => {
  return <Button variant="contained" color="primary">Add User</Button>;
};

export default memo(AddUser);
