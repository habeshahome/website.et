import React from 'react'

export default function MockupCode(props){
    return (
        <div className="mockup-code">
            <pre data-prefix="$"><code> digitial transformation</code></pre> 
            <pre data-prefix=">" className="text-warning"><code>working on technology...</code></pre> 
            <pre data-prefix=">" className="text-warning"><code>connecting with customers...</code></pre> 
            <pre data-prefix=">" className="text-warning"><code>collecting data...</code></pre> 
            <pre data-prefix=">" className="text-warning"><code>analysing data...</code></pre> 
            <pre data-prefix=">" className="text-warning"><code>getting insights...</code></pre> 
            <pre data-prefix=">" className="text-warning"><code>increasing performance</code></pre> 
            <pre data-prefix=">" className="text-success"><code>success! </code></pre>
        </div>
    )
}